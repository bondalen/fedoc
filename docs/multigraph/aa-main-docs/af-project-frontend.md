# fedoc multigraph — Frontend

**Версия:** 0.1.0  
**Дата:** 2025-11-09  
**Статус:** 🚧 В разработке  
**Авторы:** Александр

Документ фиксирует текущее состояние и планы развития frontend-подсистемы multigraph. Для общего контекста проекта см. `aa-project.md`, а для backend — `ab-project-backend.md`.

---

## 🎯 Цели и сценарии

- Визуализация графа (узлы, рёбра, связи проектов).
- Редактирование сущностей (создание/обновление блоков, дизайнов, проектов) поверх REST API.
- Поддержка realtime-обновлений через WebSocket `/ws` и интеграция с MCP Bridge.
- Единый UI-хаб для команды (debug-панели, артефакты тестирования, навигация по документации).

---

## 📦 Развертывание (план)

### Требования
- Node.js 20+ (разработка и сборка).
- npm или pnpm (по умолчанию используем npm).
- Доступ к backend API (`http://localhost:8080`) и WebSocket `/ws`.

### Локальный запуск (предполагаемый)
```bash
cd mgsrc/frontend
npm install
npm run dev -- --host

# Доступ: http://localhost:5173
```

### Production-сборка (план)
```bash
npm run build
npm run preview  # smoke-проверка
```

> Раздел будет дополнен после имплементации пайплайна сборки и деплоя.

---

## 🛠️ Разработка

### Структура проекта (план)
```
fedoc/mgsrc/frontend/
├── src/
│   ├── components/          # UI-компоненты
│   ├── composables/         # Composition API хелперы (state, hooks)
│   ├── services/            # Работа с REST API и WebSocket hub
│   ├── stores/              # Pinia-хранилища (graph, selection, projects)
│   └── styles/              # Общие стили и темы
├── public/                  # Статические ресурсы
├── index.html
├── package.json
└── vite.config.ts
```

### Текущий статус
- **Фреймворк:** Vue 3.5.22 (Composition API).
- **Сборка:** Vite 5.4.11, конфиг находится в подготовке.
- **Библиотеки визуализации:** `vis-network`, планируется интеграция с Socket.IO/WebSocket.
- **UI/UX:** макеты в процессе подготовки, предполагается использование CSS-переменных и tailwind-решения (обсуждение в работе).

### Roadmap (выдержка из `bb-tasks` раздел 2.*)
- **2.1.1** Vite-конфигурация, базовые компоненты (панель, список узлов, холст). — *(не начато)*
- **2.1.2** HTTP-клиент для API (axios/fetch), методы блоков/дизайнов/проектов. — *(не начато)*
- **2.2.1** Визуализация графа, реакция на события WebSocket. — *(не начато)*
- **2.2.2** CRUD-формы по сущностям, управление связями. — *(не начато)*
- **2.2.3** UX-полировка, уведомления, состояния загрузки. — *(не начато)*

---

## 🔌 Интеграции

- **Backend API:** REST эндпоинты `/api/blocks`, `/api/designs`, `/api/projects`, `/api/projects/:id/graph`.
- **WebSocket hub:** `ws://localhost:8080/ws`, протокол событий описан в [../cc-preliminary/25-1109/ws-channel-requirements.md](../cc-preliminary/25-1109/ws-channel-requirements.md). Реализация канала находится в разработке (block 1.3.x).
- **MCP Bridge:** планируется синхронизировать выделение и операции с фронтендом; спецификация будет расширена после задач 1.3.2.

---

## 📚 Документация и ссылки

- [aa-project.md](aa-project.md) — общий обзор проекта.
- [ab-project-backend.md](ab-project-backend.md) — состояние backend.
- [../bb-chats/](../bb-chats/) — резюме чатов (frontend-блок будет ссылаться по мере появления прогресса).
- [../cc-preliminary/25-1109/ws-channel-requirements.md](../cc-preliminary/25-1109/ws-channel-requirements.md) — протокол WebSocket.

---

## ✅ Следующие шаги

1. Подготовить шаблон проекта Vite + Vue, добавить зависимость `vis-network`.
2. Реализовать HTTP-клиент и базовые типы данных (совместимые с backend API).
3. Подключить WebSocket hub и отобразить push-обновления (зависят от задач backend 1.3.x).
4. Настроить e2e-тесты (Cypress) и Storybook/Chromatic для компонентов (план).

