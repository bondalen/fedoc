# Chat Resume — 2025-11-09 13:15

## Контекст
- Продолжение задачи из предыдущего чата: усилить негативные сценарии после реализации Projects API.

## Что сделано
1. **Негативные проверки Projects API**
   - Добавлены тесты на `404` при обращении к несуществующим проектам (`GET`, `PATCH`).
   - Проверяется конфликт имён при обновлении (`PATCH` с повторяющимся `name`), а также валидация `POST` без `name` (`422`).
2. **Графовые кейсы**
   - Интеграционный сценарий подтверждает корректную работу `/api/projects/<id>/graph`, когда связанные рёбра удалены — API возвращает пустой список без `500`.
3. **Регрессия**
   - Полный прогон `pytest -m integration` (16 тестов) — статус `passed`.

## Итог
- Негативные и деградационные сценарии для проектов покрыты, поведение API устойчиво к отсутствующим данным.

## Следующие шаги
1. Рассмотреть аналогичные тесты для ручек `/api/blocks` и `/api/designs` по сценариям бизнес-конфликтов.
2. Вынести повторяющееся создание блоков/дизайнов в вспомогательные утилиты (fixtures/helpers), чтобы сократить тестовый код.

