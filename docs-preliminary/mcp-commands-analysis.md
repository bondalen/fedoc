# Анализ действий с графом и предложения по доработке MCP команд

**Дата:** 2025-11-03  
**Автор:** AI Assistant  
**Контекст:** Анализ повторяющихся операций с графом в чате после миграции на edge_projects

---

## 📊 Анализ выполненных действий

### 1. Повторяющиеся операции в этом чате

#### 1.1. Работа с выделенными объектами
- ✅ Получение выделенных узлов через `node_get_selected`
- ✅ Получение выделенных рёбер через `node_get_selected`
- ✅ Проверка наличия связей между выделенными узлами
- ✅ Проверка проектов, связанных с выделенными рёбрами

#### 1.2. Массовые операции с проектами
- ❌ **Массовое удаление проектов из рёбер** (выполнялось через SQL)
  - Удаление fepro и femsq из нескольких рёбер одновременно
- ✅ Добавление проекта к одному ребру (через SQL функцию)
- ❌ **Массовое добавление проекта к нескольким рёбрам** (выполнялось через цикл SQL)
  - Добавление fepro к 3 рёбрам одновременно

#### 1.3. Аналитические операции
- ❌ **Проверка связей между узлами с детальной информацией** (выполнялось через SQL)
  - Получение информации о связи (from, to, rel_type)
  - Проверка проектов для связи
  - Проверка наличия связи в обоих направлениях
- ❌ **Получение информации о рёбрах с проектами** (выполнялось через SQL)
  - Список всех проектов для ребра
  - Проверка существования связи для конкретного проекта

---

## 🎯 Выявленные проблемы

### Проблема 1: Отсутствие команд для работы с проектами рёбер
**Текущее состояние:**
- Команды `edge_add`, `edge_update` принимают `projects` как параметр
- Нет отдельных команд для управления проектами существующих рёбер
- Массовые операции требуют прямых SQL запросов

**Влияние:**
- Необходимость ручного выполнения SQL для добавления/удаления проектов
- Высокий риск ошибок при массовых операциях
- Невозможность проверки проектов рёбра через MCP

### Проблема 2: Отсутствие аналитических команд
**Текущее состояние:**
- `edge_check_uniqueness` проверяет только наличие связи
- Нет команды для получения полной информации о связи (узлы, тип, проекты)
- Нет команды для проверки связей между выделенными узлами

**Влияние:**
- Необходимость выполнения SQL для получения полной информации
- Дублирование логики проверки в каждом чате

### Проблема 3: Отсутствие массовых операций
**Текущее состояние:**
- Все операции работают с одним объектом за раз
- Нет команд для массового добавления/удаления проектов

**Влияние:**
- Необходимость циклов или множественных вызовов
- Медленная работа при большом количестве объектов

---

## 💡 Предложения по новым MCP командам

### Команда 1: `edge_get_projects`
**Назначение:** Получить список проектов, связанных с ребром

**Параметры:**
- `edge_id` (required): ID ребра

**Возвращает:**
```json
{
  "success": true,
  "edge_id": "1125899906842676",
  "projects": [
    {
      "key": "fepro",
      "name": "FEPRO",
      "role": "participant",
      "weight": 1.0
    }
  ]
}
```

**Использование:**
```
"Получи проекты для ребра 1125899906842676"
"Какие проекты связаны с этим ребром?"
```

---

### Команда 2: `edge_add_project`
**Назначение:** Добавить проект к существующему ребру

**Параметры:**
- `edge_id` (required): ID ребра
- `project_key` (required): Ключ проекта (fepro, femsq, fedoc)
- `role` (optional): Роль проекта в связи (default: "participant")
- `weight` (optional): Вес связи (default: 1.0)

**Возвращает:**
```json
{
  "success": true,
  "message": "Проект fepro добавлен к ребру 1125899906842676",
  "edge_id": "1125899906842676",
  "project_key": "fepro"
}
```

**Использование:**
```
"Добавь проект fepro к ребру 1125899906842676"
"Свяжи это ребро с проектом fepro"
```

---

### Команда 3: `edge_remove_project`
**Назначение:** Удалить проект из ребра

**Параметры:**
- `edge_id` (required): ID ребра
- `project_key` (required): Ключ проекта для удаления

**Возвращает:**
```json
{
  "success": true,
  "message": "Проект fepro удалён из ребра 1125899906842676",
  "edge_id": "1125899906842676",
  "project_key": "fepro"
}
```

**Использование:**
```
"Удали проект fepro из этого ребра"
"fepro не использует эту связь, убери его"
```

---

### Команда 4: `edge_batch_add_projects`
**Назначение:** Массовое добавление проекта к нескольким рёбрам

**Параметры:**
- `edge_ids` (required): Массив ID рёбер
- `project_key` (required): Ключ проекта для добавления
- `role` (optional): Роль проекта
- `weight` (optional): Вес связи

**Возвращает:**
```json
{
  "success": true,
  "project_key": "fepro",
  "total": 3,
  "added": 3,
  "skipped": 0,
  "errors": [],
  "results": [
    {"edge_id": "1125899906842673", "status": "added"},
    {"edge_id": "1125899906842677", "status": "added"},
    {"edge_id": "1125899906842678", "status": "added"}
  ]
}
```

**Использование:**
```
"Добавь fepro ко всем выделенным рёбрам"
"Свяжи эти рёбра с проектом fepro"
```

---

### Команда 5: `edge_batch_remove_projects`
**Назначение:** Массовое удаление проекта из нескольких рёбер

**Параметры:**
- `edge_ids` (required): Массив ID рёбер
- `project_key` (required): Ключ проекта для удаления

**Возвращает:**
```json
{
  "success": true,
  "project_key": "fepro",
  "total": 3,
  "removed": 3,
  "not_found": 0,
  "errors": []
}
```

**Использование:**
```
"Удали fepro из всех выделенных рёбер"
"fepro и femsq не используют эти связи, убери их"
```

---

### Команда 6: `edge_get_info`
**Назначение:** Получить полную информацию о ребре (узлы, тип связи, проекты)

**Параметры:**
- `edge_id` (required): ID ребра

**Возвращает:**
```json
{
  "success": true,
  "edge": {
    "edge_id": "1125899906842676",
    "from": {
      "id": "844424930132002",
      "key": "t:spring-boot",
      "name": "Spring Boot",
      "kind": "product"
    },
    "to": {
      "id": "844424930131981",
      "key": "c:backend-security",
      "name": "Безопасность бакенда",
      "kind": "concept"
    },
    "relation_type": "uses",
    "projects": ["fepro"]
  }
}
```

**Использование:**
```
"Получи информацию об этом ребре"
"Покажи детали связи"
```

---

### Команда 7: `nodes_check_connection`
**Назначение:** Проверить наличие связи между двумя узлами с детальной информацией

**Параметры:**
- `from_node` (required): ID или ключ исходного узла
- `to_node` (required): ID или ключ целевого узла
- `project_filter` (optional): Фильтр по проекту

**Возвращает:**
```json
{
  "connected": true,
  "edge": {
    "edge_id": "1125899906842676",
    "from_node": "844424930132002",
    "to_node": "844424930131981",
    "relation_type": "uses",
    "projects": ["fepro"]
  },
  "for_project": {
    "fepro": true,
    "femsq": false
  }
}
```

**Использование:**
```
"Проверь связь между этими узлами"
"Есть ли связь для проекта fepro?"
```

---

### Команда 8: `selected_analyze_connections`
**Назначение:** Анализ связей между выделенными узлами (для выделенных в Graph Viewer)

**Параметры:**
- `project_filter` (optional): Фильтр по проекту

**Возвращает:**
```json
{
  "success": true,
  "nodes": [
    {"id": "844424930132002", "key": "t:spring-boot"},
    {"id": "844424930131981", "key": "c:backend-security"}
  ],
  "connections": [
    {
      "edge_id": "1125899906842676",
      "from": "844424930132002",
      "to": "844424930131981",
      "relation_type": "uses",
      "projects": ["fepro"],
      "has_for_project": {"fepro": true, "femsq": false}
    }
  ],
  "suggestions": [
    {
      "from": "844424930132002",
      "to": "844424930131981",
      "suggestion": "Связь существует, но не связана с femsq"
    }
  ]
}
```

**Использование:**
```
"Проанализируй связи между выделенными узлами"
"Какие связи есть между этими узлами для fepro?"
```

---

## 🔧 Доработка существующих команд

### Доработка 1: `node_get_selected`
**Текущее состояние:** Возвращает только базовую информацию

**Предложение:** Добавить опциональный параметр `include_projects`:
- Если `true`, для каждого ребра включать список проектов
- Использовать функцию `get_edge_projects_array` для получения проектов

**Пример:**
```json
{
  "edges": [
    {
      "id": "1125899906842676",
      "from": "844424930132002",
      "to": "844424930131981",
      "projects": ["fepro", "femsq"]
    }
  ]
}
```

---

### Доработка 2: `edge_check_uniqueness`
**Текущее состояние:** Только проверка наличия

**Предложение:** Расширить возвращаемую информацию:
- Если связь существует, вернуть `edge_id`
- Вернуть список проектов для существующей связи
- Предложить добавить проект, если его нет

**Пример:**
```json
{
  "is_unique": false,
  "existing_edge": {
    "edge_id": "1125899906842676",
    "projects": ["fepro"]
  },
  "suggestion": "Связь существует. Добавить проект femsq?"
}
```

---

## 📝 Приоритеты реализации

### Высокий приоритет (критично для текущей работы):
1. ✅ `edge_get_projects` - базовая операция проверки
2. ✅ `edge_add_project` - часто используемая операция
3. ✅ `edge_remove_project` - часто используемая операция
4. ✅ `nodes_check_connection` - аналитическая операция

### Средний приоритет (улучшает UX):
5. ✅ `edge_get_info` - детальная информация о ребре
6. ✅ `edge_batch_add_projects` - массовые операции
7. ✅ `edge_batch_remove_projects` - массовые операции
8. ✅ Доработка `node_get_selected` с проектами

### Низкий приоритет (удобство):
9. ✅ `selected_analyze_connections` - комплексный анализ
10. ✅ Доработка `edge_check_uniqueness` с предложениями

---

## 🚀 План реализации

### Этап 1: Базовые команды для проектов (1-2 часа)
- [ ] Реализовать `edge_get_projects`
- [ ] Реализовать `edge_add_project`
- [ ] Реализовать `edge_remove_project`
- [ ] Добавить обработчики в `server.py`
- [ ] Протестировать на реальных данных

### Этап 2: Аналитические команды (1 час)
- [ ] Реализовать `edge_get_info`
- [ ] Реализовать `nodes_check_connection`
- [ ] Добавить обработчики в `server.py`
- [ ] Протестировать

### Этап 3: Массовые операции (1 час)
- [ ] Реализовать `edge_batch_add_projects`
- [ ] Реализовать `edge_batch_remove_projects`
- [ ] Добавить валидацию и обработку ошибок
- [ ] Протестировать

### Этап 4: Улучшения существующих команд (30 мин)
- [ ] Доработать `node_get_selected` с проектами
- [ ] Доработать `edge_check_uniqueness` с деталями
- [ ] Обновить документацию

### Этап 5: Комплексная аналитика (1 час)
- [ ] Реализовать `selected_analyze_connections`
- [ ] Добавить логику предложений
- [ ] Протестировать с реальными данными

---

## 📚 Примеры использования новых команд

### Сценарий 1: Добавление проекта к выделенным рёбрам
```
1. node_get_selected → получить выделенные рёбра
2. Для каждого ребра:
   - edge_get_projects → проверить текущие проекты
   - edge_add_project → добавить fepro (если нет)
```

### Сценарий 2: Массовое удаление проекта
```
1. node_get_selected → получить выделенные рёбра
2. edge_batch_remove_projects → удалить fepro и femsq из всех
```

### Сценарий 3: Анализ связей между узлами
```
1. node_get_selected → получить выделенные узлы
2. selected_analyze_connections → получить все связи и предложения
3. Для каждой связи:
   - Проверить проекты
   - Добавить недостающие проекты
```

---

## 🎯 Ожидаемые результаты

После реализации всех команд:

✅ **Устранение необходимости SQL запросов** для работы с проектами рёбер  
✅ **Ускорение работы** с графом через MCP команды  
✅ **Снижение ошибок** при массовых операциях  
✅ **Улучшение UX** через аналитические команды  
✅ **Автоматизация** повторяющихся операций  

---

## 📌 Заключение

Анализ показал, что текущий набор MCP команд недостаточен для эффективной работы с проектами рёбер. Предложенные команды закрывают основные сценарии использования и позволяют полностью работать с графом через MCP без необходимости прямых SQL запросов.

Приоритет отдаётся базовым операциям работы с проектами (`edge_add_project`, `edge_remove_project`), которые используются наиболее часто.

