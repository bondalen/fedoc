# Анализ интеграции лучших практик из проекта FEMSQ в fedoc

**Дата**: 2025-11-05  
**Автор**: Александр  
**Статус**: Аналитический документ  
**Приоритет**: Высокий

---

## Контекст

В рамках совершенствования архитектуры проекта fedoc проведён детальный анализ проекта FEMSQ (Fish Eye MS SQL Server) с целью выявления лучших практик хранения информации о компонентах, модулях и артефактах проекта.

**Цель анализа**: Интегрировать лучшие решения из FEMSQ в fedoc для создания универсальной системы управления проектной документацией и метаданными.

---

## Обзор проекта FEMSQ

### Основные характеристики

**Назначение**: Система работы с контрагентами и объектами при капитальном строительстве  
**Технологический стек**:
- Backend: Java 21 LTS + Spring Boot 3.4.5
- Frontend: Vue.js 3.4.21 + Quasar Framework 2.16.1
- Database: MS SQL Server (внешняя)
- Архитектурный паттерн: Modular Monolith (ADR-007)

### Система документации

FEMSQ использует три взаимосвязанных JSON-файла:

1. **project-docs.json** — Проектная документация (архитектура, модули)
2. **project-development.json** — Планирование разработки (задачи)
3. **project-journal.json** — Дневник разработки (история)

---

## Ключевые находки

### 1. Рекурсивная иерархия модулей

**Три базовые сущности** с возможностью бесконечной вложенности:

#### Module (Модуль)
- Может содержать: `modules[]`, `components[]`, `classes[]`
- Представляет: Maven modules, проекты, пакеты верхнего уровня
- Типы: `maven-module`, `gradle-module`, `spring-boot-app`, `npm-package`, `vue-module`

#### Component (Компонент)
- Может содержать: `components[]`, `classes[]`
- Представляет: Java packages, Vue components, пакеты приложения
- Типы: `java-package`, `vue-components`, `vue-composables`, `typescript-modules`

#### Class (Класс)
- Может содержать: `classes[]` (вложенные классы)
- Представляет: Java classes/interfaces/enums, Vue components, TypeScript modules
- Типы: `java-class`, `java-interface`, `java-enum`, `vue-component`, `typescript-class`

**Пример структуры**:
```
Module (Backend)
├── modules[]
│   └── Module (Database)
│       ├── components[]
│       │   └── Component (config)
│       │       └── classes[]
│       │           └── Class (DatabaseConfigService)
│       │               └── classes[]
│       │                   └── Class (ConfigValidator - nested)
```

### 2. Группировка артефактов в объект `artifact`

**Критически важная находка**: Физические атрибуты группируются в отдельный объект для улучшения читаемости.

**Структура**:
```json
{
  "attributes": {
    "id": "DatabaseConfigService",
    "number": "01.01.01.01",
    "name": "DatabaseConfigService",
    "description": "Сервис для работы с конфигурацией БД",
    "status": "planned",
    
    "artifact": {
      "location": "com.femsq.database.config.DatabaseConfigService",
      "file_path": "src/main/java/com/femsq/database/config/DatabaseConfigService.java",
      "type": "java-class",
      "artifact_type": "file"
    },
    
    "responsibilities": [...],
    "dependsOn": [...]
  }
}
```

**Преимущества группировки**:
- ✅ Можно сворачивать в IDE (улучшенная навигация)
- ✅ Четкое разделение: метаданные vs физическое представление
- ✅ Логическая группировка атрибутов
- ✅ Масштабируемость (легко добавлять новые атрибуты)

**Атрибут `artifact_type`**:
- `"directory"` — Module или Component (папка)
- `"file"` — Class (файл)
- `"nested"` — Вложенный класс (внутри файла)

### 3. Система структурной нумерации 01-zz

**Идентична системе fedoc!**

**Формат**: 01-99, 0a-9z, a0-z9, aa-zz  
**Ёмкость**: 1,295 позиций на уровень  
**Иерархия**: Через точку (01.01.02.03)

**Применение**:
```
01. Backend Module
  01.01. Database Module
    01.01.01. Configuration Package
      01.01.01.01. DatabaseConfigService
        01.01.01.01.01. ConfigValidator (nested class)
```

**Правила**:
- Модули: `01`, `02`, `0a`, `0b`
- Компоненты: `01.01`, `01.02`, `01.0a`
- Классы: `01.01.01`, `01.01.02`
- Вложенные классы: `01.01.01.01`, `01.01.01.02`

### 4. Система расширений (extensions)

**Принцип**: Основной файл ссылается на файлы расширений для уменьшения размера и улучшения модульности.

**Структура**:
```json
{
  "modules": {
    "extensions": [
      "docs/project/extensions/modules/modules.json"
    ]
  },
  "database": {
    "extensions": [
      "docs/project/extensions/database/connection-config.json",
      "docs/project/extensions/database/authentication-methods.json"
    ]
  }
}
```

**Организация папок**:
```
docs/project/extensions/
├── modules/
│   ├── modules-example.json        # Шаблон
│   └── modules.json                # Реальные данные
├── deployment/
├── database/
├── ui/
├── network/
└── monitoring/
```

### 5. Система ссылок через иерархические пути

Все связи между файлами используют формат `type:path`.

**Таблица форматов**:

| Тип | Формат | Пример |
|-----|--------|--------|
| Модуль | `module:id` | `module:backend` |
| Компонент | `component:module.component` | `component:backend.database-config` |
| Класс | `class:module.component.class` | `class:backend.database-config.DatabaseConfigService` |
| Вложенный класс | `class:module.component.class.nested` | `class:backend.config.Service.Validator` |
| Таблица БД | `table:name` | `table:contractors` |
| Задача | `task:id` | `task:0004` |
| Чат | `chat:id` | `chat:chat-2025-10-09-001` |
| Правило AI | `rule:id` | `rule:coding-standards` |

**Пример использования**:
```json
// В project-development.json (задача)
{
  "links": {
    "projectDocsRefs": [
      "class:backend.database-config.DatabaseConfigService"
    ],
    "journalRefs": [
      "chat:chat-2025-10-09-001"
    ]
  }
}

// В project-journal.json (чат)
{
  "links": {
    "developmentTasks": ["task:0004"],
    "projectDocsRefs": [
      "class:backend.database-config.DatabaseConfigService",
      "table:contractors"
    ]
  }
}
```

**Преимущества**:
- ✅ Устойчивость (не зависит от порядка элементов)
- ✅ Уникальность (путь через точку гарантирует)
- ✅ Читаемость (понятна структура)
- ✅ Соответствие коду (похоже на пути пакетов Java)
- ✅ Гибкость (можно ссылаться на любой уровень)

### 6. Универсальный справочник типов артефактов

Документ **project-docs-json-rules-af-types.md** определяет стандартизированные типы для разных технологий.

**Backend: Java/Spring**:
- Module types: `maven-module`, `maven-submodule`, `gradle-module`, `spring-boot-app`
- Component types: `java-package`
- Class types: `java-class`, `java-interface`, `java-enum`, `java-record`, `nested-class`

**Frontend: Vue.js**:
- Module types: `npm-package`, `vue-module`
- Component types: `vue-components`, `vue-composables`, `vue-stores`
- Class types: `vue-component`, `typescript-class`, `typescript-interface`

**Расширяемость**: Легко добавлять новые технологии (Python, React, Angular и т.д.)

### 7. Обязательный чеклист при создании артефакта

**При создании нового модуля/компонента/класса**:

1. ✅ Создать файл или папку артефакта
2. ✅ Добавить в `project-docs.json` (или `modules.json`)
3. ✅ Создать задачу в `project-development.json`
4. ✅ Создать пункт структуры с `structureId` и `taskId`
5. ✅ Добавить запись в `project-journal.json`
6. ✅ Обновить зависимости (`dependsOn`)

**Контроль целостности ссылок**:
- Все ссылки проверяются перед коммитом
- Используется система типизированных ссылок
- Автоматическая валидация структуры

---

## Сравнение с подходом fedoc

### Общее

| Аспект | FEMSQ | fedoc |
|--------|-------|-------|
| **Система нумерации** | 01-zz (1295/уровень) | 01-zz (1295/уровень) |
| **Иерархия** | Module→Component→Class | Граф: узлы и рёбра |
| **Метаданные** | JSON объекты | Properties узлов |
| **Ссылки** | `type:path` | ID узлов, рёбра |

### Различия

| Аспект | FEMSQ | fedoc |
|--------|-------|-------|
| **Хранилище** | JSON файлы | PostgreSQL + Apache AGE (граф) |
| **Формат** | Рекурсивные объекты | Узлы и рёбра графа |
| **Расширяемость** | Файлы расширений | Метаузлы в графе |
| **Визуализация** | PlantUML диаграммы | Graph Viewer (vis-network) |
| **Группировка** | `artifact` объект | `properties` узлов |
| **MCP интеграция** | Нет | 23+ команд |
| **AI доступ** | Чтение файлов | Команды MCP |
| **Версионирование** | Git | История в БД |

---

## Предложения по интеграции в fedoc

### 1. Добавить группировку `artifact` в метаузлы классов

**Текущее состояние** (концепция из metadata-in-graph-architecture.md):
```
cls:GraphObject
  ├─→ prop:GraphObject.id
  ├─→ prop:GraphObject.created_at
  └─→ meth:GraphObject.to_dict
```

**Предлагаемое улучшение**:
```
cls:GraphObject
  ├─→ artifact:GraphObject.metadata
  │     properties: {
  │       location: "models.base.GraphObject",
  │       file_path: "src/lib/graph_viewer/backend/models/base.py",
  │       type: "python-class",
  │       artifact_type: "file"
  │     }
  ├─→ prop:GraphObject.id
  └─→ meth:GraphObject.to_dict
```

**Преимущества**:
- Четкое разделение метаданных и физических атрибутов
- Возможность сворачивания в Graph Viewer
- Соответствие лучшим практикам FEMSQ

### 2. Реализовать рекурсивную структуру Module→Component→Class

**Новые типы узлов**:
- `mod:*` (module) — модуль с конфигурацией сборки
- `cmp:*` (component) — компонент (пакет/директория)
- Существующий `cls:*` (class) — класс/файл

**Структура в графе**:
```
c:project (fedoc)
  c:dev-objects
    mod:backend (Backend Module)
      mod:database (Database Module)
        cmp:config (Configuration Package)
          cls:DatabaseConfigService
            cls:ConfigValidator (nested)
```

**Атрибуты узлов модулей**:
```json
{
  "node_type": "module",
  "module_type": "maven-module",
  "build_file": "pom.xml",
  "artifact": {
    "location": "femsq-backend/",
    "file_path": "femsq-backend/",
    "type": "maven-module",
    "artifact_type": "directory"
  }
}
```

### 3. Создать универсальный справочник типов в графе

**Новый метаузел**:
```
c:meta
  c:artifact-types (Справочник типов артефактов)
    c:backend-types
      c:java-types
        c:module-types
          - maven-module
          - gradle-module
        c:component-types
          - java-package
        c:class-types
          - java-class
          - java-interface
          - java-enum
    c:frontend-types
      c:vue-types
        c:module-types
          - vue-module
          - npm-package
        c:component-types
          - vue-components
        c:class-types
          - vue-component
```

### 4. Расширить систему ссылок в графе

**Добавить support для иерархических путей**:

```python
# В graph_traverse_down
def resolve_reference(ref: str) -> dict:
    """
    Разрешить ссылку формата type:path
    
    Examples:
        "module:backend" → узел mod:backend
        "class:backend.config.Service" → узел cls:Service в mod:backend/cmp:config
    """
    ref_type, path = ref.split(':', 1)
    parts = path.split('.')
    
    # Поиск узла по иерархическому пути
    return find_node_by_path(ref_type, parts)
```

### 5. Добавить MCP команды для работы с модулями

**Новые команды**:
```python
@mcp_tool()
def module_create(
    module_key: str,
    module_name: str,
    module_type: str,  # maven-module, npm-package, etc.
    parent_module: Optional[str] = None,
    artifact: dict = {}
) -> dict:
    """Создать модуль в графе"""

@mcp_tool()
def component_create(
    component_key: str,
    component_name: str,
    component_type: str,  # java-package, vue-components, etc.
    parent_module: str,
    artifact: dict = {}
) -> dict:
    """Создать компонент в графе"""

@mcp_tool()
def artifact_update(
    node_key: str,
    artifact: dict
) -> dict:
    """Обновить artifact метаданные узла"""
```

### 6. Интегрировать чеклист в workflow

**При создании артефакта через MCP**:
1. Создать узел в графе (`node_create`)
2. Добавить artifact метаданные (`artifact_update`)
3. Создать связи с родительским модулем/компонентом
4. Обновить зависимости (`dependsOn` → рёбра графа)
5. Логировать в journal (можно через отдельную таблицу)

**Автоматическая валидация**:
- Проверка уникальности ключей
- Проверка корректности типов
- Проверка существования зависимостей

---

## План реализации

### Этап 1: Подготовка структуры метаданных (2-3 дня)

**Задачи**:
1. ✅ Создать узлы `c:artifact-types` в графе
2. ✅ Добавить справочник типов для Java/Spring и Vue.js
3. ✅ Расширить `graph_traverse_down` для вывода справочника
4. ✅ Документировать структуру

**Результат**: Справочник типов артефактов в графе

### Этап 2: Реализация группировки artifact (2-3 дня)

**Задачи**:
1. ✅ Добавить поддержку вложенных свойств в узлах
2. ✅ Создать узлы `artifact:*` как дочерние для классов
3. ✅ Обновить `graph_traverse_down` для вывода artifact
4. ✅ Обновить Graph Viewer для отображения artifact

**Результат**: Классы имеют группированные artifact метаданные

### Этап 3: Рекурсивная структура Module→Component→Class (3-4 дня)

**Задачи**:
1. ✅ Добавить типы узлов `mod:*`, `cmp:*`
2. ✅ Реализовать MCP команды `module_create`, `component_create`
3. ✅ Обновить `graph_traverse_down` для обхода модулей
4. ✅ Создать миграцию существующих данных

**Результат**: Полная поддержка рекурсивной иерархии

### Этап 4: Система иерархических ссылок (2-3 дня)

**Задачи**:
1. ✅ Реализовать функцию `resolve_reference(ref: str)`
2. ✅ Добавить поддержку в `graph_traverse_down`
3. ✅ Обновить MCP команды для работы со ссылками
4. ✅ Документировать формат ссылок

**Результат**: Работающая система иерархических ссылок

### Этап 5: Интеграция чеклиста (2 дня)

**Задачи**:
1. ✅ Реализовать автоматическую валидацию
2. ✅ Добавить проверку зависимостей
3. ✅ Создать workflow для добавления артефактов
4. ✅ Документировать процесс

**Результат**: Автоматизированный процесс добавления артефактов

### Этап 6: Тестирование и документация (2-3 дня)

**Задачи**:
1. ✅ Создать тестовый проект с модулями
2. ✅ Протестировать все MCP команды
3. ✅ Обновить `.cursorrules`
4. ✅ Написать руководство пользователя

**Результат**: Полностью документированная система

**Итого**: 13-18 дней

---

## Преимущества интеграции

### Для fedoc

✅ **Универсальность** — поддержка любых технологических стеков  
✅ **Масштабируемость** — легко добавлять новые типы артефактов  
✅ **Читаемость** — группировка artifact улучшает навигацию  
✅ **Интеграция** — единая система для всех проектов  
✅ **AI-friendly** — структурированные метаданные для AI

### Для femsq

✅ **MCP интеграция** — возможность использовать mcp-fedoc  
✅ **Визуализация** — Graph Viewer для структуры проекта  
✅ **Версионирование** — история изменений в БД  
✅ **Автоматизация** — генерация кода из графа  
✅ **Централизация** — единый источник истины

---

## Риски и ограничения

### Риски

⚠️ **Сложность миграции** — существующие данные fedoc нужно переструктурировать  
⚠️ **Обучение** — команде нужно изучить новую структуру  
⚠️ **Производительность** — запросы к графу могут быть медленнее JSON  
⚠️ **Совместимость** — нужно поддерживать оба подхода в переходный период

### Ограничения

📌 **PostgreSQL + AGE** — зависимость от конкретной БД  
📌 **Размер графа** — большое количество метаузлов может замедлить запросы  
📌 **Сложность запросов** — Cypher сложнее, чем чтение JSON

---

## Следующие шаги

### Немедленные действия (следующий чат)

1. **Начать с Этапа 1**: Создать справочник типов артефактов
   ```python
   node_create(
       node_key="c:artifact-types",
       node_name="Справочник типов артефактов",
       node_type="concept"
   )
   ```

2. **Изучить существующую структуру**: Проанализировать текущие метаузлы в fedoc

3. **Создать прототип**: Добавить 1-2 модуля с новой структурой

4. **Протестировать**: Проверить работу с Graph Viewer

### Среднесрочные цели (1-2 недели)

1. Реализовать все этапы плана
2. Мигрировать существующие данные
3. Обновить документацию
4. Обучить команду

### Долгосрочные цели (1-2 месяца)

1. Интегрировать femsq с mcp-fedoc
2. Создать генератор кода из графа
3. Разработать систему автоматической синхронизации
4. Расширить на другие проекты (fepro)

---

## Заключение

Анализ проекта FEMSQ выявил ряд отличных практик, которые могут значительно улучшить архитектуру fedoc:

1. **Группировка artifact** — улучшает читаемость и навигацию
2. **Рекурсивная структура** — универсальный подход к описанию проектов
3. **Справочник типов** — стандартизация для разных технологий
4. **Иерархические ссылки** — удобная система связей
5. **Чеклист артефактов** — автоматизация процесса

Интеграция этих практик в fedoc создаст мощную универсальную систему управления проектной документацией и метаданными, которая сможет обслуживать проекты на любых технологических стеках.

**Ключевое преимущество**: В будущем femsq будет разрабатываться с использованием mcp-fedoc, создавая единую экосистему инструментов разработки.

---

**Документ создан**: 2025-11-05  
**Автор**: Александр  
**Версия**: 1.0  
**Статус**: Готов к реализации

