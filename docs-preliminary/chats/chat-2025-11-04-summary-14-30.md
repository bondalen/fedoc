# Резюме чата: 2025-11-04 14:30

**Дата**: 2025-11-04  
**Время**: 14:30  
**Автор**: Александр  
**Тема**: Исправление SVG экспорта и предложение архитектуры с метаданными в графе

---

## Краткое содержание

Чат начался с диагностики проблем SVG экспорта, перешёл к обнаружению расхождений стилей между canvas и SVG, и завершился комплексным архитектурным предложением по хранению метаданных классов в самом графе.

**Ключевые темы**:
1. ✅ Исправлены проблемы SVG экспорта (текст + кривизна)
2. ❌ Обнаружено расхождение стилей (canvas vs SVG)
3. 💡 Предложена иерархия классов GraphObject
4. 💡 Предложена синхронизация фронтенд-бакенд
5. ✅ Финализировано: Метаданные в графе (философия fedoc)

---

## Проблемы и решения

### 1. SVG экспорт: нечитаемый текст ✅

**Проблема**: Обводка текста (stroke) перекрывала заливку (fill).

**Решение**: Двойной слой текста:
```javascript
// Слой 1: обводка (под текстом)
<text fill="none" stroke="${fontStrokeColor}" stroke-width="${fontStrokeWidth}">
  ${label}
</text>
// Слой 2: основной текст
<text fill="${fontColor}">
  ${label}
</text>
```

**Файл**: `src/lib/graph_viewer/frontend/src/utils/exportToSVG.js` (строки 162-192)

---

### 2. SVG экспорт: отсутствие кривизны рёбер ✅

**Проблема**: Рёбра отрисовывались прямыми линиями.

**Решение**: 
- Исправлено чтение `visualizationConfig.edges.smooth`
- Улучшен расчёт контрольных точек кривой Безье
- Учёт направления движения (вверх/вниз, влево/вправо)

**Файл**: `src/lib/graph_viewer/frontend/src/utils/exportToSVG.js` (строки 200-247)

---

### 3. Расхождение стилей canvas и SVG ❌

**Обнаружено**: Стили берутся из трёх независимых источников:
1. `visualization.json` — частично используется
2. `GraphCanvas.vue` — хардкод опций vis-network (строки 24-86)
3. `stores/graph.js` — `applyTheme()` перезаписывает стили (строки 296-339)

**Последствия**: Canvas и SVG выглядят по-разному.

**Решение**: Предложена архитектура с иерархией классов (см. ниже).

---

## Архитектурные предложения

### Эволюция предложений

```
Этап 1: Иерархия классов на фронтенде
  ↓
Этап 2: Синхронизация фронтенд-бакенд через YAML
  ↓
Этап 3: Метаданные в графе (ФИНАЛЬНОЕ) ✅
```

### Финальное решение: Метаданные в графе

**Основная идея**: Хранить определения всех классов в самом графе как специальные метаузлы.

**Новые типы узлов**:
- `m:*` (meta) — метаданные
- `cls:*` (class-def) — определение класса
- `prop:*` (property-def) — определение свойства
- `meth:*` (method-def) — определение метода
- `rel:*` (relation-def) — определение связи

**Структура в графе**:
```
c:project (fedoc)
  c:meta (Метаданные проекта)
    c:class-system (Система классов)
      cls:GraphObject
        prop:GraphObject.id
        meth:GraphObject.to_dict
      cls:GraphVertex
        prop:GraphVertex.key
        meth:GraphVertex.infer_type
      cls:Project (Бизнес-класс)
        prop:Project.name
        rel:Project.has_backends
        meth:Project.get_backends
      cls:Backend (Бизнес-класс)
        prop:Backend.framework
        meth:Backend.get_project
    c:visualization-config
      c:node-styles
      c:edge-styles
    c:api-contract
      c:endpoint-get-graph
```

**Интеграция с MCP**:
```python
# Расширение команды
graph_traverse_down(project="fedoc", include_metadata=true)

# Новые команды
class_create(class_key, class_name, extends, ...)
class_add_property(class_key, property_name, property_type, ...)
class_add_method(class_key, method_name, returns, ...)
class_regenerate_code(class_key)
```

**Генератор кода**:
```bash
# Читает граф, генерирует Python и JavaScript классы
python tools/generate_from_graph.py
```

---

## Созданные документы

### Основной документ с подробным описанием
📄 [`chat-2025-11-04-svg-export-fixes-and-architecture-proposal.md`](../architecture-proposals/chat-2025-11-04-svg-export-fixes-and-architecture-proposal.md)
- Полное описание всех проблем и решений
- Архитектурные предложения
- План реализации (10-15 дней)
- Следующие шаги

### Детальные документы

📄 [`metadata-in-graph-architecture.md`](../architecture-proposals/metadata-in-graph-architecture.md)
- Полное описание финального решения
- Структура метаузлов в графе
- Расширение `graph_traverse_down`
- MCP команды для работы с классами
- Генератор кода из графа
- Обновление `.cursorrules`

📄 [`graph-object-class-hierarchy.md`](../architecture-proposals/graph-object-class-hierarchy.md)
- Иерархия классов GraphObject
- Детальное описание каждого класса
- Методы рендеринга (vis-network, SVG)
- Примеры кода для фронтенда

📄 [`frontend-backend-class-sync.md`](../architecture-proposals/frontend-backend-class-sync.md)
- Промежуточное предложение с YAML (архивное)
- Причины отклонения
- Миграция к финальному решению
- Сравнительная таблица подходов

---

## Изменённые файлы

### В текущем чате

1. **`src/lib/graph_viewer/frontend/src/utils/exportToSVG.js`**
   - ✅ Исправлен рендеринг текста (двойной слой)
   - ✅ Улучшен расчёт кривых Безье
   - ✅ Исправлено чтение конфига кривизны

### Для будущих изменений

**При реализации архитектуры**:

1. `src/lib/graph_viewer/frontend/src/components/GraphCanvas.vue`
   - Убрать хардкод опций vis-network
   - Делегировать в VisualizationManager

2. `src/lib/graph_viewer/frontend/src/stores/graph.js`
   - Упростить `applyTheme()`
   - Использовать классы для визуализации

3. `src/mcp_server/handlers/graph_traverse_down.py`
   - Добавить параметр `include_metadata`
   - Реализовать обход метаузлов

4. `.cursorrules`
   - Обновить: `graph_traverse_down(project="fedoc", include_metadata=true)`

**Новые файлы для создания**:

**Backend**:
- `src/lib/graph_viewer/backend/models/base.py` (GraphObject)
- `src/lib/graph_viewer/backend/models/vertices/` (GraphVertex, ConceptVertex, ...)
- `src/lib/graph_viewer/backend/models/business/` (Project, Backend, ...)

**Frontend**:
- `src/lib/graph_viewer/frontend/src/models/base.js`
- `src/lib/graph_viewer/frontend/src/models/vertices/`
- `src/lib/graph_viewer/frontend/src/models/business/`
- `src/lib/graph_viewer/frontend/src/services/VisualizationManager.js`

**Инструменты**:
- `tools/generate_from_graph.py` (генератор из графа)
- `tools/templates/python_class.jinja2`
- `tools/templates/js_class.jinja2`
- `src/mcp_server/handlers/class_operations.py` (MCP команды)

---

## План реализации

### Этап 1: Подготовка графа (1-2 дня)
- [ ] Создать узлы `c:meta`, `c:class-system` в графе
- [ ] Добавить базовые определения классов
- [ ] Добавить определения бизнес-классов
- [ ] Расширить `graph_traverse_down` для метаданных

### Этап 2: Генератор из графа (2-3 дня)
- [ ] Реализовать `GraphSchemaReader`
- [ ] Создать шаблоны Jinja2
- [ ] Реализовать `CodeGenerator`
- [ ] Протестировать генерацию

### Этап 3: MCP команды (1-2 дня)
- [ ] `class_create()`
- [ ] `class_add_property()`
- [ ] `class_add_method()`
- [ ] `class_regenerate_code()`

### Этап 4: Миграция (2-3 дня)
- [ ] Перенести существующие классы в граф
- [ ] Сгенерировать код
- [ ] Проверить работоспособность

### Этап 5: Рефакторинг визуализации (2-3 дня)
- [ ] Создать структуру `models/`, `services/`
- [ ] Реализовать базовые классы
- [ ] Убрать хардкод из `GraphCanvas.vue` и `graph.js`

### Этап 6: Тестирование (2 дня)
- [ ] AI ассистент с метаданными
- [ ] Синхронизация фронт-бэк
- [ ] Визуализация в Graph Viewer
- [ ] Canvas и SVG консистентность

**Итого**: 10-15 дней

---

## Для следующего чата

### Что делать дальше

1. **Начать с Этапа 1**: Подготовка графа
   ```python
   # Создать метаузлы в БД
   INSERT INTO canonical_nodes (key, name, ...) VALUES ('c:meta', 'Метаданные', ...)
   INSERT INTO canonical_nodes (key, name, ...) VALUES ('c:class-system', 'Система классов', ...)
   ```

2. **Прочитать детальные документы**:
   - [`metadata-in-graph-architecture.md`](../architecture-proposals/metadata-in-graph-architecture.md) — основная архитектура ⭐
   - [`graph-object-class-hierarchy.md`](../architecture-proposals/graph-object-class-hierarchy.md) — иерархия классов
   - [`frontend-backend-class-sync.md`](../architecture-proposals/frontend-backend-class-sync.md) — история эволюции

3. **Ключевые вопросы**:
   - Какие базовые классы добавить первыми?
   - Как организовать метаузлы в графе?
   - Начать с полной миграции или постепенной?

### Команды для начала работы

```bash
# 1. Получить контекст (когда метаданные будут готовы)
graph_traverse_down(project="fedoc", include_metadata=true)

# 2. Создать первый класс в графе
class_create(
  class_key="cls:GraphObject",
  class_name="GraphObject",
  abstract=true,
  backend_module="models.base",
  frontend_module="models/base.js"
)

# 3. Сгенерировать код
class_regenerate_code()
```

---

## Преимущества финального решения

✅ **Единственный источник правды**: Граф  
✅ **AI всегда актуален**: Метаданные при каждом запуске  
✅ **Версионирование**: История в графе  
✅ **Нет дублирования**: Никаких YAML/JSON  
✅ **MCP интеграция**: AI может менять схему  
✅ **Визуализация**: Классы в Graph Viewer  
✅ **Философия fedoc**: Всё в графе, на клиенте только секреты

---

## Контекст проекта

**Проект**: fedoc  
**Цель**: Централизованная система правил и документации  
**Философия**: Граф — единственный источник истины  
**Технологии**: Python 3.12, PostgreSQL 16 + Apache AGE, Vue.js 3, vis-network  

---

## Ссылки

### Документы
- [Полное описание чата](../architecture-proposals/chat-2025-11-04-svg-export-fixes-and-architecture-proposal.md)
- [Архитектура метаданных в графе](../architecture-proposals/metadata-in-graph-architecture.md) ⭐
- [Иерархия классов GraphObject](../architecture-proposals/graph-object-class-hierarchy.md)
- [История: Синхронизация фронтенд-бакенд](../architecture-proposals/frontend-backend-class-sync.md)

### Код
- `src/lib/graph_viewer/frontend/src/utils/exportToSVG.js` (исправлен)
- `src/lib/graph_viewer/frontend/src/components/GraphCanvas.vue` (требует изменений)
- `src/lib/graph_viewer/frontend/src/stores/graph.js` (требует изменений)
- `src/mcp_server/handlers/graph_traverse_down.py` (требует расширения)

### Инструменты для создания
- `tools/generate_from_graph.py`
- `tools/templates/python_class.jinja2`
- `tools/templates/js_class.jinja2`
- `src/mcp_server/handlers/class_operations.py`

---

**Статус**: ✅ Готово к реализации  
**Приоритет**: 🔥 Высокий  
**Сложность**: ⭐⭐⭐ Средняя (10-15 дней)  
**Влияние**: 🎯 Критическое (архитектурная основа проекта)

