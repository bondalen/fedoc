# Резюме чата: Реструктуризация графа и исправление функций Graph Viewer

**Дата:** 2025-11-03  
**Статус:** ✅ В процессе (requires next session)  
**Автор:** Александр  
**AI:** Claude (Auto)

---

## 📋 Полное резюме чата

### 🎯 Основная цель
Реструктуризация графа проекта для корректного отображения архитектуры базы данных, миграций и реактивности в проектах fedoc, fepro и femsq.

### 🔍 Исходная проблема
1. **Упрощённая структура БД** - прямая связь backend → database
2. **Неправильная реактивность** - нет правильной иерархии абстракций
3. **Ошибки в фильтрации проектов** - функции Graph Viewer не работали с проектом fepro
4. **Двойное хранение** - проекты в e.projects и edge_projects

---

## 🚀 Основные задачи и решения

### 1. **Интеграция архитектуры базы данных**

#### Созданные концепты и технологии:
- ✅ `c:database-access` - Доступ к БД
- ✅ `c:orm` - ORM (Object-Relational Mapping)
- ✅ `c:database-driver` - Драйвер БД
- ✅ `c:backend-database` - База данных бэкенда
- ✅ `c:database-migrations` - Миграции базы данных (переименовано из c:backend-migrations)
- ✅ `t:postgresql-jdbc` - PostgreSQL JDBC Driver
- ✅ `t:spring-data-jpa` - Spring Data JPA
- ✅ `t:mssql-jdbc` - MS SQL Server JDBC Driver
- ✅ `t:project-reactor` - Project Reactor
- ✅ `t:spring-data-r2dbc` - Spring Data R2DBC
- ✅ `t:r2dbc-postgresql` - R2DBC PostgreSQL Driver
- ✅ `t:r2dbc-spec` - R2DBC Specification (переименовано из t:r2dbc)

#### Построенная архитектура для fepro:
```
Spring Boot
  └─ c:database-access
      ├─ c:orm
      │   └─ t:spring-data-r2dbc
      │       └─ c:database-driver
      │           └─ t:r2dbc-postgresql
      │               └─ t:r2dbc-spec
      │               └─ c:backend-database
      │                   └─ t:postgresql@16
      │
      └─ c:database-migrations
          └─ t:liquibase
              └─ c:database-driver
                  └─ t:postgresql-jdbc
                      └─ c:backend-database
                          └─ t:postgresql@16
```

---

### 2. **Реструктуризация реактивности**

#### Созданные концепты:
- ✅ `c:reactivity` - Реактивность (абстрактный концепт)
- ✅ `c:reactive-streams` - Reactive Streams
- ✅ `c:reactive-driver-concept` - Реактивный драйвер
- ✅ `c:tech-stack` - Стек технологий
- ✅ `c:full-reactive-stack` - Полный реактивный стек
- ✅ `c:frontend-reactive` - Реактивность фронтенда

#### Правильная иерархия:
```
c:reactivity (абстракция)
  ├─ c:reactive-streams
  │   └─ t:project-reactor
  └─ t:r2dbc-spec
      └─ c:reactive-driver-concept
          └─ t:r2dbc-postgresql

c:web-app (fepro)
  └─ c:tech-stack
      └─ c:full-reactive-stack
          ├─ implements → c:reactivity
          ├─ includes → c:backend-reactive
          └─ includes → c:frontend-reactive

c:backend (fepro)
  └─ c:backend-reactive
      ├─ uses → t:project-reactor
      └─ implementedBy → t:spring-data-r2dbc
          └─ uses → t:r2dbc-postgresql
```

#### Исправленные связи:
- ❌ Удалено: `Spring Boot → uses → Реактивность бакенда` (избыточная связь)
- ✅ Правильное направление: `Реактивность → implementedBy → Полный реактивный стек`
- ✅ Правильное направление: `Реактивность бакенда → implementedBy → Spring Data R2DBC`
- ✅ R2DBC размещён на уровне Реактивности бакенда (специфично для БД)

---

### 3. **Интеграция миграций**

#### Переименования:
- `c:backend-migrations` → `c:database-migrations` (более универсально)

#### Структура:
```
c:database-access (fepro)
  └─ provides → c:database-migrations
      ├─ implementedBy → t:liquibase (только fepro)
      │   └─ uses → c:database-driver
      │       └─ implementedBy → t:postgresql-jdbc
      │           └─ connectsTo → c:backend-database
      └─ implementedBy → t:alembic (без проектов - альтернатива)
```

---

### 4. **Обновление проектов в рёбрах**

#### Добавлены проекты fepro и femsq к:
- ✅ Java 21 → Фреймворк бакенда
- ✅ Все 5 рёбер цепочки фронтенда:
  1. Слой → Фронтенд
  2. Фронтенд → Фронтенд-фреймворк
  3. Фронтенд-фреймворк → Vue.js
  4. Vue.js → Инструмент сборки
  5. Инструмент сборки → Vite

**Обоснование:** Все три проекта (fedoc, fepro, femsq) используют Vue.js 3.4+ и Vite для сборки фронтенда.

---

### 5. **КРИТИЧЕСКОЕ: Исправление фильтрации проектов**

#### Проблема:
- Ошибка 500 при запросе `/api/nodes?project=fepro` и `/api/graph?project=fepro`
- Причина: Apache AGE хранит `e.projects` как JSON-строку, а не массив
- Синтаксис `WHERE 'value' IN e.projects` не поддерживается в Apache AGE 1.6.0

#### Решение:
**Фильтрация через PostgreSQL LIKE** вместо Cypher IN:

```sql
WHERE t.projects::text LIKE FORMAT('%%"%s"%%', project_filter)
```

#### Исправленные функции:
1. ✅ `get_all_graph_for_viewer()` - основная функция получения графа
2. ✅ `get_all_nodes_for_viewer()` - получение узлов
3. ✅ `get_graph_for_viewer()` - обход графа от узла
4. ✅ Обновлён файл: `dev/docker/init-scripts/postgres/06-graph-viewer-functions.sql`

#### Результат:
- ✅ API работает корректно для всех проектов
- ✅ 44 узла для fepro
- ✅ 51 ребро для fepro
- ✅ Правильная фильтрация по проектам

---

## 🔴 ОБНАРУЖЕННАЯ ПРОБЛЕМА: Двойное хранение проектов

### Текущая ситуация:
1. ✅ Таблица `public.edge_projects` создана (55 строк, 34 рёбра мигрированы)
2. ✅ Старые рёбра (до миграции) имеют данные в edge_projects
3. ❌ Новые рёбра (созданные через MCP) попадают ТОЛЬКО в `e.projects`
4. ❌ Нет синхронизации между двумя источниками
5. ❌ Расхождение: 34 ребра в таблице vs 82 ребра в графе

### Анализ вариантов хранения:

| Вариант | Плюсы | Минусы | Рекомендация |
|---------|-------|--------|--------------|
| **A. Только e.projects** | Простота, один источник | Нет защиты от дублей, медленнее на больших объёмах | ❌ |
| **B. Только edge_projects** | Foreign keys, индексы, быстрее | Нужна миграция данных и переписать функции | ✅ **РЕКОМЕНДУЮ** |
| **C. Гибрид** | Минимальные изменения | Двойное хранение, сложность | ⚠️ |

### Рекомендованное решение: Вариант B

**Преимущества edge_projects:**
- ✅ `UNIQUE(edge_id, project_id)` предотвращает дубликаты
- ✅ Foreign key на `projects.id` обеспечивает целостность
- ✅ Быстрые JOIN запросы с индексами
- ✅ Легче сложные запросы (агрегации, фильтры по нескольким проектам)
- ✅ Расширяемость: role, weight, metadata в будущем

**План реализации:**
1. Мигрировать 63 ребра из e.projects → edge_projects
2. Удалить projects из рёбер Apache AGE
3. Переписать функции Graph Viewer для JOIN с edge_projects
4. Добавить автоматическую синхронизацию при создании/обновлении рёбер

**Сложность:** Средняя (2-3 часа работы)

---

## 📂 Созданные и изменённые файлы

### Изменённые файлы:
- ✅ `dev/docker/init-scripts/postgres/06-graph-viewer-functions.sql`
  - Исправлены функции фильтрации: `get_all_graph_for_viewer`, `get_all_nodes_for_viewer`, `get_graph_for_viewer`
  - Фильтрация через PostgreSQL LIKE вместо Cypher IN

### Использованные артефакты:
- ✅ `docs-preliminary/chats/chat-2025-10-26-normalized-projects-architecture.md`
  - Архитектура нормализованного хранения проектов в edge_projects
- ✅ `dev/docker/init-scripts/postgres/001_create_normalized_projects.sql` (старая миграция)
- ✅ `dev/docker/init-scripts/postgres/002_create_project_functions.sql` (старая миграция)
- ✅ `dev/docker/init-scripts/postgres/003_migrate_*.sql` (старые миграции)
- ✅ `src/lib/graph_viewer/backend/edge_validator_age.py`
  - Функция `format_properties_for_cypher` - форматирование свойств для Cypher
  - Функция `insert_edge_safely` - создание рёбер
- ✅ `src/lib/graph_viewer/backend/project_enricher.py`
  - Мёртвый код для работы с edge_projects (не используется)
- ✅ Проект fepro: `/home/alex/fepro/src/app/frontend/package.json`
  - Подтверждение использования Vue 3.4.21 и Vite 5.1.4
- ✅ Проект femsq: `/home/alex/femsq/docs/project/project-docs.json`
  - Подтверждение планируемой архитектуры с Vue 3.4.21 и Vite

---

## 🎯 Следующие шаги (для следующего чата)

### 🔴 КРИТИЧНОЕ: Завершить миграцию на edge_projects

#### Шаг 1: Миграция данных
```python
# Задача: Мигрировать 63 ребра из e.projects → edge_projects
# Файл: scripts/migrate_projects_to_edge_table.py (создать)
# Логика:
# 1. Получить все рёбра из графа
# 2. Для каждого ребра извлечь e.projects
# 3. Добавить в edge_projects через add_project_to_edge()
```

#### Шаг 2: Переписать функции Graph Viewer
```sql
-- Задача: Использовать JOIN с edge_projects вместо e.projects
-- Файл: dev/docker/init-scripts/postgres/06-graph-viewer-functions.sql

-- Пример для get_all_graph_for_viewer:
SELECT e.edge_id, e.from_id, e.to_id, ...
FROM cypher(...) AS e
JOIN public.edge_projects ep ON e.edge_id::bigint = ep.edge_id
JOIN public.projects p ON ep.project_id = p.id
WHERE p.key = project_filter
```

#### Шаг 3: Автоматическая синхронизация
```python
# Задача: При создании/обновлении ребра добавлять в edge_projects
# Файл: src/lib/graph_viewer/backend/edge_validator_age.py

def insert_edge_safely(...):
    # После создания ребра в графе:
    edge_id = result['edge_id']
    
    # Добавить проекты в edge_projects
    for project_key in properties.get('projects', []):
        cur.execute("""
            SELECT ag_catalog.add_project_to_edge(%s, %s, 'participant', 1.0)
        """, (edge_id, project_key))
```

#### Шаг 4: Удалить projects из рёбер
```sql
-- После успешной миграции:
-- Удалить поле projects из всех рёбер или оставить NULL

UPDATE common_project_graph.project_relation
SET properties = properties - 'projects';
```

### ✅ ОПЦИОНАЛЬНОЕ: Улучшения

1. **Очистка кода:**
   - Удалить или очистить `src/lib/graph_viewer/backend/project_enricher.py`
   - Удалить старые миграции 001, 002, 003 (после успешной миграции)

2. **Документация:**
   - Создать `docs/migrations-to-edge-projects.md` с описанием архитектуры
   - Обновить `.cursorrules` с новыми принципами работы с проектами

---

## 📊 Статистика сессии

### Создано узлов:
- 12 новых концептов и технологий для БД
- 6 новых концептов для реактивности
- Всего: **18 новых узлов**

### Обновлено рёбер:
- ~25 рёбер обновлено (добавлены проекты, изменены направления)
- ~7 рёбер удалено (избыточные связи)
- ~20 рёбер создано (новые связи)

### Исправлено функций:
- 3 функции PostgreSQL для Graph Viewer
- Исправлен синтаксис фильтрации проектов

---

## 🎓 Выученные уроки

1. **Apache AGE ограничения:**
   - `IN` оператор не поддерживается для agtype массивов
   - `projects` хранится как JSON-строка, не как массив
   - Решение: PostgreSQL-уровневая фильтрация через LIKE

2. **Архитектура данных:**
   - JSON массивы в графе ≠ нормализованная таблица
   - Нормализованная структура лучше для целостности и производительности
   - Foreign keys критичны для предотвращения ошибок

3. **Реактивность:**
   - Важно различать абстракцию (reactivity) и конкретные реализации
   - R2DBC специфичен для БД, не для общей реактивности
   - Project Reactor = реализация Reactive Streams

---

## 📝 Ссылки на артефакты

### Изменённые файлы:
- `dev/docker/init-scripts/postgres/06-graph-viewer-functions.sql` - исправленные функции

### Документы:
- `docs-preliminary/chats/chat-2025-10-26-normalized-projects-architecture.md` - архитектура edge_projects
- `.cursorrules` - правила работы с проектом fedoc (обновить после завершения)

### Миграции (исторические):
- `src/lib/graph_viewer/backend/migrations/001_create_normalized_projects.sql`
- `src/lib/graph_viewer/backend/migrations/002_create_project_functions.sql`
- `src/lib/graph_viewer/backend/migrations/003_migrate_*.sql`

### Исходный код:
- `src/lib/graph_viewer/backend/edge_validator_age.py` - создание/обновление рёбер
- `src/lib/graph_viewer/backend/api_server_age.py` - API сервер Graph Viewer
- `src/lib/graph_viewer/backend/project_enricher.py` - мёртвый код (удалить)

### Проектные данные:
- `/home/alex/fepro/src/app/frontend/package.json` - подтверждение Vue/Vite
- `/home/alex/femsq/docs/project/project-docs.json` - архитектура femsq

---

## ⚠️ Критичные незавершённые задачи

1. **🔴 МИГРАЦИЯ НА edge_projects** (Blocking)
   - 63 ребра не синхронизированы
   - Функции Graph Viewer используют e.projects
   - Нет автоматической синхронизации при создании

2. **🟡 Производительность фильтрации**
   - Текущий метод через LIKE работает, но медленнее
   - После миграции на edge_projects будут JOIN с индексами

3. **🟡 Очистка кода**
   - Удалить project_enricher.py
   - Удалить старые миграции после успеха

---

## 🎯 Команды для быстрого старта в следующем чате

```bash
# 1. Проверить состояние
graph_traverse_down(project="fedoc")

# 2. Проверить расхождение данных
cd /home/alex/fedoc && python3 -c "
import psycopg2
conn = psycopg2.connect(host='localhost', port=15432, database='fedoc', user='postgres', password='fedoc_test_2025')
cur = conn.cursor()
cur.execute('LOAD age; SET search_path = ag_catalog, public;')

# Проверить количество
query1 = 'SELECT COUNT(DISTINCT edge_id) FROM public.edge_projects'
cur.execute(query1)
in_table = cur.fetchone()[0]

query2 = 'SELECT COUNT(*) FROM ag_catalog.get_all_graph_for_viewer(NULL)'
cur.execute(query2)
in_graph = cur.fetchone()[0]

print(f'В таблице: {in_table}, В графе: {in_graph}, Разница: {in_graph - in_table}')
"

# 3. Создать скрипт миграции
# TODO: создать scripts/migrate_projects_to_edge_table.py
```

---

## 📌 Ключевые решения

1. ✅ **R2DBC** размещён на уровне "Реактивность бакенда", а не общей "Реактивности"
2. ✅ **Миграции БД** в концепте `c:database-migrations` (не backend-migrations)
3. ✅ **Полный реактивный стек** связывает frontend и backend реактивность
4. ✅ **Project Reactor** как реализация Reactive Streams
5. ✅ **Vue.js и Vite** используются всеми тремя проектами
6. ✅ **Фильтрация через PostgreSQL** для Apache AGE ограничений
7. ✅ **edge_projects** выбран как единственный источник истины (TODO: миграция)

---

**Статус:** 🟡 В процессе - требуется завершить миграцию на edge_projects в следующем чате

