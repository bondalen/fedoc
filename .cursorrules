# Правила работы с проектом fedoc

## 🔴 КРИТИЧЕСКИ ВАЖНО: Контекст проекта

### В начале КАЖДОГО чата выполнить:
```
graph_traverse_down(project="fedoc")
```

**Почему это важно**:
- Граф проекта — единственный источник истины о структуре
- Без контекста AI не знает актуальную архитектуру
- Изменения в графе происходят регулярно
- Контекст помогает избежать ошибок и несоответствий

### Что делать с выводом:
1. **Изучить иерархию**: Понять, как организован проект
2. **Найти нужный компонент**: Использовать нумерацию 01-zz
3. **Проверить зависимости**: Увидеть связи между компонентами
4. **Узнать технологии**: Стек, версии, порты

### Когда обновлять контекст:
- ✅ Начало нового чата (ОБЯЗАТЕЛЬНО)
- ✅ После изменений в графе
- ✅ При работе с незнакомой частью проекта
- ✅ При возникновении вопросов об архитектуре

---

## Принципы работы

### sys-001: Обход графа вниз
Документация проекта формируется путём обхода графа от узла вниз по исходящим рёбрам. Команда `graph_traverse_down` реализует этот принцип.

**Применение**: Получение полной картины проекта или его части.

### sys-002: Обход графа вверх (будущее)
Для получения контекста конкретного артефакта (файла, класса) будет команда `graph_traverse_up`.

**Применение**: Сборка правил для работы над конкретным артефактом.

---

## Нумерация элементов

Используется система **01-zz** (принцип dev-001):
- Формат: 01-99, 0a-9z, a0-z9, aa-zz
- Ёмкость: 1295 позиций на уровень
- Иерархия: через точку (01.01.02.03)

**Пример**:
```
01. c:project (Проект)
  01.01. c:dev-objects (Объекты разработки)
    01.01.01. c:mcp-server (MCP-сервер)
      01.01.01.01. t:mcp-fedoc (MCP-сервер fedoc)
```

---

## Работа с графом

### Просмотр графа
- `graph_viewer_open()` — открыть веб-интерфейс
- `graph_traverse_down(project="fedoc")` — получить текстовое описание

### Добавление узлов и рёбер
- `node_create()` — создать новый узел
- `edge_add()` — добавить связь между узлами
- `edge_check_uniqueness()` — проверить уникальность связи

### Обновление
- `node_update()` — обновить свойства узла
- `edge_update()` — обновить связь

### Удаление
- `node_delete()` — удалить узел (если нет связей)
- `edge_delete()` — удалить связь

### Проверка изменений
После изменений в графе **обязательно** обновить контекст:
```
graph_traverse_down(project="fedoc")
```

---

## Технологический стек

### Backend
- **Язык**: Python 3.12
- **База данных**: PostgreSQL 16 + Apache AGE (графовое расширение)
- **API**: REST (Flask)

### Frontend
- **Фреймворк**: Vue.js 3.5.22
- **Сборка**: Vite 5.4.11
- **Визуализация**: vis-network, Socket.IO

### MCP-сервер
- **Язык**: Python 3.10+
- **Протокол**: Model Context Protocol
- **Порт**: 15000
- **Стек**: Flask-SocketIO, psycopg2, python-arango

### Инструменты
- **IDE**: Cursor (AI-интегрированная)
- **Graph Viewer**: Веб-интерфейс для визуализации графа

---

## Структура проекта

```
fedoc/
├── src/
│   ├── lib/
│   │   ├── graph_traversal/      # Библиотека обхода графа (sys-001)
│   │   └── graph_viewer/         # Graph Viewer (backend + frontend)
│   └── mcp_server/               # MCP-сервер
│       ├── handlers/             # Обработчики команд
│       └── server.py             # Главный файл сервера
├── docs/                         # Документация
├── docs-preliminary/             # Рабочие документы и резюме чатов
├── config/                       # Конфигурация
└── .cursorrules                  # Этот файл

```

---

## Команды MCP

### Обязательные (в начале чата)
- `graph_traverse_down(project="fedoc")` — получить структуру проекта

### Визуализация
- `graph_viewer_open()` — открыть Graph Viewer
- `graph_viewer_status()` — проверить статус
- `graph_viewer_stop()` — остановить

### Работа с узлами
- `node_create()` — создать узел
- `node_update()` — обновить узел
- `node_delete()` — удалить узел
- `node_get_selected()` — получить выбранные в Graph Viewer

### Работа с рёбрами
- `edge_add()` — добавить связь
- `edge_update()` — обновить связь
- `edge_delete()` — удалить связь
- `edge_check_uniqueness()` — проверить уникальность

### Диагностика
- `imports_check()` — проверить импорты
- `arango_test()` — проверить ArangoDB
- `ssh_test()` — проверить SSH туннель

---

## Примеры использования

### Начало работы
```
1. graph_traverse_down(project="fedoc")
2. Изучить вывод
3. Начать работу с пониманием структуры
```

### Добавление новой технологии
```
1. node_create(node_key="t:new-tech", node_name="New Technology", node_type="technology")
2. edge_add(from_node="c:backend", to_node="t:new-tech", relation_type="uses", projects=["fedoc"])
3. graph_traverse_down(project="fedoc")  # Проверить изменения
```

### Обновление узла
```
1. node_update(node_id="t:vue", properties={"version": "3.6.0"})
2. graph_traverse_down(project="fedoc")  # Проверить изменения
```

---

## Важные файлы

### Документация
- `docs-preliminary/graph-traverse-down-algorithm.md` — алгоритм sys-001
- `docs-preliminary/graph-traverse-down-summary.md` — итоги ручного обхода
- `docs-preliminary/project-overview-generated.md` — обзор проекта (устаревает)
- `docs-preliminary/chats/` — резюме предыдущих чатов

### Код
- `src/lib/graph_traversal/` — библиотека обхода графа
- `src/mcp_server/handlers/graph_traverse_down.py` — MCP-команда
- `src/mcp_server/server.py` — главный файл MCP-сервера

---

## Контакты и поддержка

**Автор**: Александр  
**Проект**: fedoc (Централизованная система правил и документации)  
**Версия**: 1.0.0  
**Дата обновления**: 2025-11-01

